sudo -u postgres psql

CREATE DATABASE alpha_portal;

CREATE USER alpha_portal_user WITH PASSWORD '21Besikama)!';

ALTER ROLE alpha_portal_user SET client_encoding TO 'utf8';
ALTER ROLE alpha_portal_user SET default_transaction_isolation TO 'read committed';
ALTER ROLE alpha_portal_user SET timezone TO 'UTC';

GRANT ALL PRIVILEGES ON DATABASE alpha_portal TO alpha_portal_user;

Lanjut PostGIS
\c alpha_portal

CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS postgis_topology;
\q

GRANT ALL ON SCHEMA public TO alpha_portal_user;
ALTER SCHEMA public OWNER TO alpha_portal_user;

===== clone to vps =====
cd ~
git clone https://github.com/21bria/alpha-portal-backend.git
cd alpha-portal-backend


nano .env
==================
SECRET_KEY=GANTI_SECRET_KEY

DEBUG=False

ALLOWED_HOSTS=localhost,127.0.0.1,api.portal.kawi-nickel.com

DB_NAME=alpha_portal
DB_USER=alpha_portal_user
DB_PASSWORD=21Besikama)!
DB_HOST=localhost
DB_PORT=5432

==============
sudo apt install python3-venv -y
====

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

===========
python3 -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

==============